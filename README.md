# Лабораторная работа № 1 — Программная инженерия

**Тема:** Знакомство с системой управления версиями Git и сервисом GitHub
**Студент:** гр. 025, Eдмель Х.
**Преподаватель:** Зоркальцев А. А.
**ТУСУР, Томск, 2026**

## Отчёт

Полный отчёт о лабораторной работе: [Otchet_LR1_Git.docx](Otchet_LR1_Git.docx)

## Содержимое репозитория

| Файл | Описание |
|---|---|
| `fichier.txt` | текстовый файл, созданный в ветви `main` |
| `notes.txt` | текстовый файл, созданный в ветви `ma-branche` |
| `images/` | снимки экрана, использованные в отчёте |

## Выполненные операции

- настройка Git (`user.name`, `user.email`, `init.defaultBranch`);
- создание локального репозитория (`git init`);
- фиксация изменений (`git add`, `git commit`);
- создание ветви и работа в ней (`git checkout -b`);
- подключение удалённого репозитория (`git remote add origin`);
- отправка и получение изменений (`git push`, `git pull`);
- слияние ветвей (`git merge`, режим Fast-forward).

Итоговая история содержит 6 коммитов и 2 ветви: `main` и `ma-branche`.
